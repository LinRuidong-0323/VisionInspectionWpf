<Window x:Class="VisionInspection.Views.SerialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="串口通讯" Height="550" Width="750"
        WindowStartupLocation="CenterOwner" MinHeight="400" MinWidth="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: 串口配置 -->
        <Border Grid.Row="0" Padding="10,8" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <WrapPanel>
                <TextBlock Text="端口:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox Name="CmbPort" Width="80" Margin="0,0,10,0"/>

                <TextBlock Text="波特率:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox Name="CmbBaud" Width="90" Margin="0,0,10,0" SelectedIndex="5">
                    <ComboBoxItem Content="9600"/><ComboBoxItem Content="19200"/><ComboBoxItem Content="38400"/>
                    <ComboBoxItem Content="57600"/><ComboBoxItem Content="115200"/><ComboBoxItem Content="9600"/>
                </ComboBox>

                <TextBlock Text="数据位:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox Name="CmbDataBits" Width="55" Margin="0,0,10,0" SelectedIndex="3">
                    <ComboBoxItem Content="5"/><ComboBoxItem Content="6"/><ComboBoxItem Content="7"/><ComboBoxItem Content="8"/>
                </ComboBox>

                <TextBlock Text="停止位:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox Name="CmbStopBits" Width="55" Margin="0,0,10,0" SelectedIndex="0">
                    <ComboBoxItem Content="1"/><ComboBoxItem Content="1.5"/><ComboBoxItem Content="2"/>
                </ComboBox>

                <TextBlock Text="校验:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox Name="CmbParity" Width="70" Margin="0,0,10,0" SelectedIndex="0">
                    <ComboBoxItem Content="None"/><ComboBoxItem Content="Odd"/><ComboBoxItem Content="Even"/>
                </ComboBox>

                <Button Name="BtnOpen" Content="▶ 打开" Width="65" Click="BtnOpen_Click"
                        Background="#28A745" Foreground="White" FontWeight="Bold" Margin="0,0,8,0"/>
                <Button Name="BtnClose" Content="⏹ 关闭" Width="65" Click="BtnClose_Click"
                        IsEnabled="False" Background="#DC3545" Foreground="White"/>

                <Button Name="BtnRefresh" Content="刷新" Width="50" Click="BtnRefresh_Click" Margin="8,0,0,0"/>
            </WrapPanel>
        </Border>

        <!-- Row 1: 发送区 -->
        <DockPanel Grid.Row="1" Margin="10,6,10,4">
            <Button Name="BtnSend" Content="发送" Width="60" DockPanel.Dock="Right" Click="BtnSend_Click"
                    Background="#0078D7" Foreground="White"/>
            <TextBox Name="TxtSend" Height="28" KeyDown="TxtSend_KeyDown"/>
        </DockPanel>

        <!-- Row 2: 收发记录 -->
        <DataGrid Name="GridLog" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" FontSize="11" RowHeight="20" Margin="10,0,10,4">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Width="120" Binding="{Binding Time}"/>
                <DataGridTextColumn Header="方向" Width="55" Binding="{Binding Direction}"/>
                <DataGridTextColumn Header="HEX" Width="220" Binding="{Binding Hex}"/>
                <DataGridTextColumn Header="ASCII/文本" Width="*" Binding="{Binding Ascii}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Row 3: 详细日志 -->
        <DockPanel Grid.Row="3" Margin="10,4,10,10">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,4">
                <Button Content="清空记录" Width="80" Height="26" Click="BtnClear_Click"/>
            </StackPanel>
            <TextBox Name="TxtDetail" IsReadOnly="True" FontFamily="Consolas" FontSize="10"
                     VerticalScrollBarVisibility="Auto" Background="White" Foreground="#333"/>
        </DockPanel>
    </Grid>
</Window>
